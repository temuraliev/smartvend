# Screenshots

Add final screenshots here before GitHub submission.

Required screenshots:

- `circuit_design.png`: CircuitVerse circuit with labels visible.
- `python_output.png`: Python program output showing a test case or the built-in test results.

Suggested Python screenshot:

Run:

```bash
python src/main.py --test
```

Take a screenshot after all tests pass.

This folder also includes:

- `python_output_sample.txt`: sample output from the built-in tests.
- `circuit_design_placeholder.svg`: a block diagram guide. Replace it with an actual CircuitVerse screenshot for the final submission.
